#include <stdio.h>
#include <stdlib.h>
#include "cliente.h"
#include "string.h"
#include "utn.h"
#define CANTIDAD_CLIENTES 100

int main()
{
    Cliente* clientes[CANTIDAD_CLIENTES];
    Cliente* aux;
    char auxNombre[64];
    aux = cliente_newCliente();
    if(aux!=NULL)
    {
        cliente_setNombre(aux, "Leandro");
        clientes[0] = aux;
        if()cliente_getNombre(aux, auxNombre);
        printf("%s", auxNombre);
    }
    return 0;
}
